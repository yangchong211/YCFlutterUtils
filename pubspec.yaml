name: yc_flutter_plugin
description: A new Flutter plugin.
version: 0.0.1
author:
homepage:

environment:
  sdk: ">=2.7.0 <3.0.0"
  flutter: ">=1.20.0"

dependencies:
  flutter:
    sdk: flutter
  # https://github.com/a14n/dart-decimal
  decimal: "0.3.5"
  # https://github.com/dart-lang/crypto
  crypto: "2.1.5"
  # https://github.com/dart-lang/convert
  convert: "2.1.1"


dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  plugin:
    platforms:
      android:
        package: com.yc.yc_flutter_plugin
        pluginClass: YcFlutterPlugin
      ios:
        pluginClass: YcFlutterPlugin
